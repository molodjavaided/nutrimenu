/**
 * Motion-пресеты проекта. Единые кривые/тайминги, чтобы анимации ощущались согласованно.
 * Растить по мере появления новых паттернов (fade-slide, stagger, tap).
 * См. память: project_motion_presets.
 */

/** Базовая «плавная» кривая для мелких переходов (поворот шеврона, hover). */
export const SMOOTH = { duration: 0.28, ease: [0.4, 0, 0.2, 1] as const }

/** Раскрытие/сворачивание тел секций — мягче и длиннее SMOOTH, без резкости на высоком контенте. */
export const REVEAL = { duration: 0.42, ease: [0.22, 1, 0.36, 1] as const }
