---
date: 2026-07-14
tags: [daily]
---

# Сессия 1 — Инструментарий + Fable-ревью + AI-SAFE аудит + генуборка

## ✅ Что сделали / зашипано
**Ничего не закоммичено и не задеплоено.** Изменения на диске (некоммичены), tsc чист на каждом шаге.

**Ранбук MyResult (Фазы 1-5):**
- 4 скилла скопированы + адаптированы под NutriMenu: `ai-safe` (стек + 3 профиля угроз проекта),
  `wrap-session` (убран синк доски/CRM → auto-memory), `ux-researcher-designer` (ссылки + ICP),
  `impeccable` (RU-copy врезка).
- `vault/` создан (10-Daily/30-Permanent/40-MOC) + `vault/40-MOC/HOME.md`.
- CLAUDE.md: раздел «Рабочее соглашение» (DoR/DoD, WIP≤2, office-hours, probe_model, check_env, wrap-session).
- `scripts/probe_model.mjs` + `scripts/check_env.mjs` (check_env протестирован — работает).

**Fable-5 ревью — разобрано:** `docs/REPORT_2026-07-14.md` (код+рынок), `PLAN_OPUS_2026-07.md`, `FABLE5_GUIDE.md`.

**AI-SAFE AUDIT** → `SECURITY-AISAFE.md` (композит ~7.5/10). 2 фикса применены:
- JSON-LD XSS в `app/menu/[slug]/page.tsx:161` — экранирование `<>&` (было: `JSON.stringify` не экранирует `</script>`).
- Лимит размера vision-входа в `app/api/parse-pdf-ttk` (10 МБ base64 → 413).
- Отложен: hardening имени в meta-промпте (низкий импакт).

**Генуборка фич:**
- Удалено: sales-бот `lib/sales-lead-bot.ts` + `sales-classify.ts` + cron `bot-learn` + запись в vercel.json;
  `lib/credentialsStore.ts` (0 ссылок); `app/(auth)/login`+`register` (легаси); dep `driver.js` (lock синхронизирован).
- **Миграция DROP** 3 таблиц sales-бота (`SalesLead`/`BotConversationLog`/`BotKnowledge`) применена на Neon
  через serverless-адаптер. Было 1+1+0 строк (стейл тест-данные, юзер подтвердил drop).
- **Квота AI-lookup** (штрихкод+мета): поля User `aiLookup*`/`bonusAiLookups`, `plans.ts` (TEST10/START60/STD300/CUSTOM∞,
  триал=10 acquisition), `lib/ai-lookup-quota.ts`, гейт в lookup-barcode (только Sonar-fallback) + lookup-meta,
  UX-сообщения о квоте. Миграция `add_ai_lookup_quota` применена на Neon (колонки на месте).

**Прочее:** graphify обновлён (2962 узла, 24 узла удалённого бота вычищены, $0.07); память `project_rf_hosting_plan.md`
(Timeweb VPS+Docker план под РФ, задачи на будущее).

## 🐞 Грабли (на будущее)
- **Prisma 7.8** требует адаптер: `new PrismaClient()` без `{ adapter }` падает. Скрипты — как `lib/db.ts` (PrismaNeon+ws).
- **`prisma migrate deploy` не достучался до Neon** (P1001, TCP:5432 закрыт в этом окружении). Serverless-адаптер
  по 443 работает → применял DROP/ALTER через `$executeRawUnsafe`. Миграц. файлы идемпотентны (IF EXISTS / IF NOT EXISTS),
  prod запишет их на след. `migrate deploy`. **Не проверено: пишет ли Vercel-деплой историю миграций автоматически.**
- **`migrate dev --create-only` не создаёт файл** в non-interactive при data-loss варнинге → миграции писал руками.
- graphify без ключа падает на семантике доков; код-часть скан бесплатно. Дал GEMINI_API_KEY (free-tier 429 на 1 чанке — код-граф ок).

## ⏸ ГДЕ ОСТАНОВИЛИСЬ (старт след. сессии)
Выписан полный список фич (A владелец / B гость / C админ / D сквозное) — **разбираем каждую по порядку**.
Ничего не закоммичено. Перед коммитом квоты — `/qa` золотой путь (скан штрихкода + исчерпание квоты, браузерно не гонял).

## 📋 Открытые задачи
0. **Разбор фич по списку** (A→D) — главная задача след. сессии. Список в чате/можно восстановить из routes.
- `/qa`-прогон квоты AI-lookup перед коммитом (деньги/гейтинг).
- Решить судьбу большого некоммиченного трее (Sentry/token-version/observability из прошлых сессий) — закоммитить?
- Генуборка bucket D: `lib/store.ts` отвязать от боевого DishSheet; слить mock-data/demo-data.
- Проверить, пишет ли prod `migrate deploy` историю 2 новых миграций (drop_sales_bot + add_ai_lookup_quota).
- Будущее: РФ-хостинг ([[rf-hosting-plan]]), оплата ЮKassa (Этап 1 PLAN_OPUS), email-домен (Этап 2).
